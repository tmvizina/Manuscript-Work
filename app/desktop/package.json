{
  "name": "book-writer-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build:main": "tsc -p tsconfig.main.json",
    "build:preload": "esbuild ./src/preload/index.ts --bundle --platform=node --format=cjs --target=node22 --external:electron --outfile=dist/preload/index.cjs",
    "build": "npm.cmd run typecheck && npm.cmd run build:main && npm.cmd run build:preload",
    "dev": "npm.cmd --prefix ../ui run build && npm.cmd run build && electron .",
    "start": "npm.cmd run build && electron .",
    "package": "npm.cmd --prefix ../ui run build && npm.cmd run build && electron-builder --config electron-builder.yml"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "36.9.5",
    "electron-builder": "26.15.3",
    "esbuild": "0.25.0",
    "typescript": "^5.6.0"
  }
}
