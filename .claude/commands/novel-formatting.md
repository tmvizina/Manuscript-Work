---
description: Format manuscript files — consistent chapter headings, scene breaks, paragraph spacing; preserves prose untouched
argument-hint: "[files or folder to format, and target format]"
---

Invoke the `novel-formatting` skill via the Skill tool. If the skill is not installed on this machine, read `skills/novel-formatting/SKILL.md` in this repo and follow its instructions directly.

Produces clean, novel-ready Markdown or plain text while preserving prose, chapter order, scene breaks, and audiobook readability. The step before `/audiobook-text-prep-chunker`.

Request: $ARGUMENTS
